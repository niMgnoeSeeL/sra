There is no need to install specific software to run the artifact.
The artifact is self-contained and can be run on any machine with a Jupyter notebook installed.
To run 'RQ-*' notebooks. No additional data is required to run the artifact.
To run the notebooks under the 'rq2/scripts' folder, the unzipped data from the 'rq2/fuzz-data' folder is required.
